# CLAUDE.md

このファイルは、このリポジトリで Claude Code(および人間の開発者)が作業する際に従うべきルールを定義します。

## プロジェクト概要

Linux 向けの USB 書き込みツール。誤って `/` や `/boot` などのシステムディスクを破壊しないことを最優先に設計する。

## アーキテクチャ / 責務分離

以下の責務を混ぜないこと。1つのファイル・関数が複数の責務にまたがる変更を提案しないこと。

- **GUI**: ユーザーインターフェース。現時点では未実装。実装時も Core / Safety Engine のロジックをGUI層に持ち込まない。
- **Core**: アプリケーションの調整ロジック(デバイス収集 → 安全判定 → 書き込み実行のオーケストレーション)。現時点では `main.rs` がこの役割を暫定的に担っている。
- **Linux Backend** (`src/linux_backend.rs`): Linux固有の情報収集のみを行う。UDisks2 / D-Bus / `/proc` などから生データを集めて `DeviceSnapshot` を組み立てる。ここに安全判定ロジック(書き込み可否の判断)を持ち込まない。
- **Device / データモデル** (`src/device.rs`): `DeviceSnapshot` などのデータ構造の定義のみ。収集ロジックや判定ロジックを持たない。
- **Safety Engine** (`src/safety.rs`): `DeviceSnapshot` を受け取り、安全側に倒した判定(`SafetyAssessment`)を返すことのみを行う。D-Bus 呼び出しやファイルI/Oなど収集処理を持ち込まない。
- **main.rs**: 現状は PoC / テストハーネスの位置づけ。恒久的な Core 実装ではないことを前提に扱う。

新しいコードを追加する際は、上記のどの層に属するかを明確にし、層をまたぐ責務の混在を避けること。

## 安全設計の原則(最重要)

書き込み対象デバイスの判定において、以下は必ず安全側(書き込み禁止 / Caution 以上)に倒すこと。

- システムディスク、`/`、`/boot`、`/boot/efi` をマウントしているデバイス・パーティション
- アクティブな swap が乗っているデバイス
- LVM 構成要素(`LVM2_member` など)
- RAID 構成要素(`linux_raid_member`、MD RAID 関連)
- 暗号化デバイス(`crypto_LUKS`、`IdUsage=crypto`)
- 上記が複合している、あるいは構成が複雑で安全性を機械的に断定できないケース

`/dev/sdX` のようなカーネルが割り振るデバイスパスだけを、物理デバイスの Identity(同一性の根拠)として信用しないこと。デバイスパスは再割り当てされ得るため、シリアル番号・ドライブオブジェクト・WWN など、より安定した識別情報と併せて扱う。

新しい判定条件を追加する際は、「安全側に倒す」というデフォルトを崩さないこと。判定ロジックを変更する場合は、既存のテスト(あれば)と `reports/latest.md` に判定結果の変化を明記すること。

## 禁止事項

以下は Claude Code / 自動化されたエージェントが自律的に実行してはならない。

- `sudo` の実行
- パッケージの導入・削除・更新(`apt`, `pacman`, `dnf`, `cargo install` など、システムやツールチェーンに影響する操作)
- 破壊的なストレージ操作(`dd`, `mkfs`, `wipefs`, パーティション操作、実デバイスへの書き込みなど)。これはテスト目的であっても対象外。
- `git add` / `git commit` / `git push`。これらはユーザーから明示的に依頼された場合にのみ実行してよい。

## 変更後の検証

コードを変更した場合は、必ず以下を実行すること。

1. `cargo check`
2. `cargo test`

いずれかが失敗する場合は、原因を調査して報告する(黙って `--no-verify` 的な回避をしない)。

## 作業レポートのルール(`reports/latest.md`)

**今後、何らかの作業(コード変更、設定変更、調査など)を行った後は、必ず `reports/latest.md` を作成・更新すること。** 前回の内容を上書きしてよい(履歴が必要な場合はユーザーの指示に従い別ファイルに退避する)。

`reports/latest.md` には最低限、以下のセクションを含めること。

- **Task**: 依頼された作業の内容
- **Summary**: 何を行ったかの要約
- **Files changed**: 変更・追加・削除したファイルの一覧
- **Implementation**: 実装内容の詳細
- **Tests**: 実行したテスト(内容・コマンド)
- **Test results**: テストの実行結果(成功/失敗、主要な出力)
- **Warnings or limitations**: 既知の制約、警告、未対応事項
- **Git status**: 作業終了時点の `git status` の要約(コミットしたかどうかも明記)
- **Recommended next step**: 次に行うべき推奨作業

このレポートは、他の開発者や次のセッションの Claude Code(および ChatGPT 等、レビュー引き継ぎ先の他ツール)が、コードを読み直さなくても現在の状態を把握できることを目的とする。詳細は `reports/README.md` を参照。

`reports/latest.md` の取り扱いに関する追加ルール:

- `reports/latest.md` は **Git 管理対象にしない**(`.gitignore` で除外済み)。ローカルの作業引き継ぎファイルとして扱う。
- 実デバイスのシリアル番号を生値で記載しない。
- 認証情報、トークン、個人情報、その他ユーザー固有の識別情報を記録しない。
- 識別情報の記録が必要な場合は、`<redacted>` または `Device A` / `Device B` のように匿名化した表現を使うこと。
